services:
  digibyte-node:
    build: .
    container_name: digibyte
    volumes:
      - ./data:/data
      - ./digibyte.conf:/data/digibyte.conf
    restart: unless-stopped
    network_mode: "host"
